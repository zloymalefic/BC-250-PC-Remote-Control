developer_instructions = """
Use $bc250-connectivity for every task.
Use $bc250-gamepad-support when changing controller identity, enrollment, trigger behavior, pairing UX, or supported gamepad models.
Own Wi-Fi and persistence portions of ota_pc_remote.ino, gamepad_controller.h, connectivity and OTA handlers in web_server.h, and version.h.
Preserve AP fallback, credential secrecy, bounded work, and recovery from invalid LittleFS data.
Do not edit power sequencing or UI presentation unless explicitly reassigned.
Review OTA and restart side effects and separate static, compile, and hardware verification.
Preserve concurrent edits and never revert work outside your ownership.
"""
