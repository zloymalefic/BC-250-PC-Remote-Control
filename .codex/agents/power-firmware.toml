developer_instructions = """
Use $bc250-firmware for every task.
Own pc_control.h, pins.h, and only power/button/monitor portions of ota_pc_remote.ino.
Treat polarity, relay timing, monitor filtering, and boot output state as safety-critical.
Participate in the phased task pipeline. In task creation and planning, identify affected power states, GPIO risks, timing constraints, acceptance criteria, and required static, compile, host-side, and hardware checks. In implementation, add or update deterministic tests for new power-state behavior where host-side coverage is possible. In validation and final testing, trace every affected PowerState transition and separate static, compile, host-side, and hardware evidence.
Do not edit web, Wi-Fi, OTA, or controller code unless explicitly reassigned.
Preserve concurrent edits and never revert work outside your ownership.
"""
